# === 記憶體傾印 / 擷取物（可重新產生，且含專有客戶端記憶體資料，不上傳）===
dumps/

# === Ghidra 專案（280MB，含 >100MB 單檔，超過 GitHub 上限）===
ghidra_proj/

# === 專有遊戲客戶端二進位（不重新散布）===
TWClient.bin
*.bin

# === 編譯產物（原始碼 .c 保留，產物不上傳）===
*.dll
*.exe
*.o
*.obj

# === Python 快取 ===
__pycache__/
*.pyc

# === 崩潰 / 執行記錄 ===
hs_err_pid*.log
*.log
